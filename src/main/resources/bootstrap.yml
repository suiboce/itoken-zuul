spring:
  profiles: dev
  cloud:
    config:
      uri: localhost:8080
      name: zuul
      label: master
      profile: dev
---
spring:
  profiles: prod
  cloud:
    config:
      uri: http://192.168.79.135:8080
      name: zuul
      label: master
      profile: prod
